package com.oops;

import java.util.Arrays;

public class Basic {
    public static void main(String[] args) {
        Student stud1;

//        Declaration of reference variable which will reference to object of type Student

//        stud1 = new Student();
//        Dynamically allocates memory;
//        Allocates memory at the runtime and returns reference to that memory chunk

        // new keyword -> dynamic memory allocation
        //  Student stud1 = this happens at compile time (checks syntax, and converts into bytecode)
        // new Student(); -> this happens at runtime; when the program is running; memory is allocated at that time

        // We can't access reference, or manipulate it in Java
        // When we can create objects, that class is instantiated


        // The default value of primitives in classes, objects like ArrayList, Array is 0 for int, 0.0 for float, false for boolean, null for objects
        // Constructors
//        stud1 = new Student();
//        stud1.rollNo = 4040;
//        stud1.name = "Riya";
//        System.out.println(stud1.name);


    }


}
